(function($){	$(function(){		var _STORE = {			selectArea:function(url, type , pid) {				var _csrf = $('#_csrf').val();				var data = {_csrf:_csrf};				data[type] = pid;				$.ajax({			      	 type: "GET",			         url: url,			         data:data,			         success: function(json) {			        	var obj = JSON.parse(json); 			            if (parseInt(obj.errorCode) == 0) {			            				            				            				            }else{			            				            				            				            }			         }			   	});			}		};		//区域选择		$('.area-select')			.on('change','.province-sel',function(){				var provinceId = $(this).val();				_STORE.selectArea(_config.getCityUrl,'province_id',provinceId);			})			.on('change','city-sel',function(){				var cityId = $(this).val();				_STORE.selectArea(_config.geDistrictUrl,'province_id',provinceId);			})													})})(jQuery);